name: glcheck
on:
  pull_request:
  push:
    branches: [master, feature-glcheck]
  workflow_dispatch:
  
jobs:
  glcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm run glcheck
  
